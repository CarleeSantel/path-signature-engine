Streamlit app that detects market regimes from geometry of price paths with rolling windows of log price, log return, and realized volatility into path signatures (iterated integrals using rough path theory), clustering the windows with KMeans. Regimes are linked with an MLE estimated markov transition matrix and app runs a parallel HMM baseline; app sees where these two approaches disagree. 
